prototype(Queo.Forms:Field.Text) < prototype(Neos.Fusion:Component) {
    // API von aussen
    name = ''
    submittedArgument = ''
    submittedArgumentValidation = ${[]}

    // Internal implementation
//     <Neos.Fusion:Debug validation={props.submittedArgument}/>
    renderer = afx`
        <input type="text" value={props.submittedArgument} name={"params[" + props.name + "]"}/>
    `
}
