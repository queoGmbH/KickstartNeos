prototype(Queo.Forms:Field.Text) < prototype(Neos.Fusion:Component) {
    // API von aussen
    name = ''
    validation = ${[]}
    submittedArgument = ''

    // Internal implementation
    renderer = afx`
        <input type="text" value={submittedArgument} name={"params[" + props.name + "]"}/>
    `
}
