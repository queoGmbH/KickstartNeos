prototype(Queo.Forms:Field.Group) < prototype(Neos.Fusion:Component) {
    childFields = null
    childFields.@process.toJson = ${Json.stringify(value)}

    renderer = afx`
        {props.childFields}
    `
}
